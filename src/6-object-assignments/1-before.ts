export const foo = () => {
  const person = {
    name: "Jane Doe",
    species: "Human",
  };

  console.log(person.name === "Washington");
  console.log(person.species === "Vulcan");
  console.log(person.species === "Human");
};
